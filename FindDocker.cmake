find_program(docker docker)

# function(add_container tag_name)
#   cmake_parse_arguments(docker "" "VERSION" "" ${ARGN})
#   add_custom_command(COMMAND ${docker} --build --tag) 
#   add_custom_target()
# endfunction()
